import { Data, Types } from "./Types";
/**
 * The class to represent generator error
 */
export declare class GeneratorError extends Error {
    constructor(message: string);
}
/**
 * Generate the key-value pair into document
 * @param key The key as the unique
 * @param value The value to generate
 */
export declare function generate<T extends Types>(value: T, refs: Data<Types>[]): Data<T>;
/**
 * Generate value into data
 * @param value The value to generate from
 * @param refs The map to detect references
 */
export declare function generateData<T extends Types>(value: T, refs: Data<unknown>[], refStack: WeakMap<object, number>): Data<T>;
