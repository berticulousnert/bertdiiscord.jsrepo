import { Doc, Document, Data, Types } from "./Types";
/**
 * The class to represent transformer error
 */
export declare class TransformerError extends Error {
    constructor(message: string);
}
/**
 * Transform the document into higher-level document
 * @param document The document to transform from
 */
export declare function transform<T extends Types>(document: Document<T>): Doc<T>;
/**
 * Transform the data into an unknown value
 * @param data The data to transform from
 */
export declare function transformData<T extends Types>(data: Data<T>, refs: Data<unknown>[], refStack: Map<number, object>, index?: number): T;
